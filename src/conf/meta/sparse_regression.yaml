inherit:
    - ../meta_base.yaml

model:
  pos_encode: False

training:
    task: sparse_linear_regression
    task_kwargs: {"sparsity": 3}
    curriculum:
        points:
            start: 11
            end: 41
            inc: 2
            interval: 2000

out_dir: ../models/meta_sparse_linear_regression

wandb:
    name: "meta_sparse_regression"
